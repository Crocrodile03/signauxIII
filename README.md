# signauxIII

pour la gestion de fichier lourd, il faut télécharger git Large File Storage (git fls)

```
git lfs install
```

Dans le `.itattributes`, il faut ajouter les types de fichiers à gérer avec `git lfs`
